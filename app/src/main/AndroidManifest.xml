<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.penjadwalankerja">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_andra"
        android:label="Andra7208"
        android:roundIcon="@mipmap/ic_launcher_andra"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="
AIzaSyBiaG-mPGYtFUTOY50ad9rgxaS1S7BdxDY
" />

        <activity
            android:name=".GoogleMapsActivity"
            android:label="@string/title_activity_google_maps"></activity>
        <activity android:name=".Part_Admin.PilihUbahJadwal"/>
        <activity android:name=".Part_Admin.PilihBuatJadwal" />
        <activity android:name=".Part_Admin.MenuAdmin" />
        <activity android:name=".Part_User.MenuUserEdo" />
        <activity android:name=".Part_Admin.UbahJadwalEdo" />
        <activity android:name=".Part_User.LihatJadwalEdo" />
        <activity android:name=".Part_Admin.BuatJadwalEdo" />
        <activity android:name=".Tentang" />
        <activity android:name=".Chat" />
        <activity android:name=".Part_User.LihatJadwalEcha" />
        <activity android:name=".Part_Admin.UbahJadwalEcha" />
        <activity android:name=".UbahSingleJadwal.UbahSingleJadwal" />
        <activity android:name=".Part_Admin.BuatJadwalEcha" />
        <activity android:name=".Part_User.MenuUserEcha" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>